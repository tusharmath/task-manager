doctype 5
html(lang="en", ng-app="myAppModule", manifest=(debug === true ? "" : "manifest.appcache"))
	head
		title Playing around with Angular JS
		if debug
			link(rel="stylesheet", type="text/css", href="styles/less/style.css")
		else
			link(rel="stylesheet", type="text/css", href="style-" + version + ".css")
	body
		h1 Task Manager
		.container
			div
				//Category LIST
				div.category-column
					input#categoryName(placeholder="Add a category", type="text")
					button.btn#addCategoryButton Add
					div.category-list
				

				//TASKS LIST
				div.tasks-column
					div.task-list
					input#newTaskDescription(placeholder="Add a task", type="text")
					button.btn#addTaskButton Add
					

		if debug
			script(src='http://localhost:35729/livereload.js')
			script(src="tQuery.js")
			script(src="TaskManager.js")
			script(src="View.js")
			script(src="app.js")
		else
			script(src='script-' + version + '.js')
